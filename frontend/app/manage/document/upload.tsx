import React from "react";
import LocalBooksPage from "../local-books";
/** Book import is device-first for every normal upload entry point. */
export default function UploadBook(){return <LocalBooksPage/>;}
