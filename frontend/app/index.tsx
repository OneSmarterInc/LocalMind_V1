import React from "react";
import { View } from "react-native";
import { Loading, colors } from "@/ui";
export default function Index() { return <View style={{ flex: 1, backgroundColor: colors.bg, justifyContent: "center" }}><Loading /></View>; }
